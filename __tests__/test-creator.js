const creatorController = require("../controllers/creator");

describe("Creator", () => {
	describe("controller functions", () => {
		describe("dashboard", () => {
			test("if creator can't be found call res.redirect", () => {});
		});
	});
});
